<%= form_for(@todo) do |f| %>
  <% if @todo.errors.any? %>
  <div id="error_explanation">
  	<h2>
      <%= pluralize(@todo.errors.count, "error") %> prohibited this todo from being saved
  	</h2>
  	<ul>
      <% @todo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
   	</ul> 
      <% end %>
    </div>
  <% end %>

  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
  
  </p>

  <p>
    <%= f.label :due_date %>
    <%= f.date_select :due_date, { :discard_year => true} %>
  </p>

  <p>
    <%= f.label :complete %>
    <%= f.check_box(:complete) %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>

<%= link_to 'Back', todos_path %>